Place the background image you provided in this folder named exactly `bg.jpg`.

Path: frontend/public/bg.jpg

Vite will serve files in `public` at the web root, so the CSS references `/bg.jpg`.

If you prefer another filename, update `src/styles.css` background-image URL accordingly.